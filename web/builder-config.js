// BELTRIX builder monetization is intentionally disabled until an operator configures
// a real builder address and fee. Do not commit secrets here; a builder address is public.
export const BELTRIX_BUILDER=Object.freeze({
 enabled:false,
 address:null,
 perpFeeTenthsBp:0,
 spotFeeTenthsBp:0
});
